# 【NovelCrafter 进阶指南】如何精准配置 Codex Tracking（设定集追踪与匹配）

在 NovelCrafter 的创作流程中，**Tracking（追踪）** 选项卡是一个至关重要的控制中心。它不仅决定了系统如何在整部作品中检测并高亮显示设定集条目（Codex entries），还配置了这些条目如何与 AI 进行交互——即决定它们是否以及何时被纳入发送给 AI 的 Prompt 上下文（Context）中。

---

## 1. 追踪与匹配设置 (Tracking / Matching)

在 Tracking 选项卡中，最基础的设置就是 **“Track this entry by name/alias”（通过名称/别名追踪此条目）** 勾选框。它决定了 NovelCrafter 是否会在你的创作内容中主动寻找该条目的名称及其所有别名。

### 开启追踪（系统默认状态）
当该功能处于开启状态时：
* 设定条目的名称和别名会在你的手稿（Manuscript）、计划（Plan）以及代码片段（Snippets）中被添加下划线高亮显示。
* 每一次提及（Mentions）都会被自动计数，并显示在 Codex 顶部的提及追踪器（Mentions Tracker）中。
* 该条目会呈现在外观热力图（Appearance Heatmap）中。

### 关闭追踪
当关闭此功能时：
* 文本中的下划线高亮显示会立即消失。
* 该条目将被排除在提及热力图（Mention Heatmap）之外。

> 💡 **进阶小贴士 (Extra Tip)**
> 
> 在写作的不同阶段，你可以灵活地开启或关闭追踪。例如，在撰写初稿时保持追踪开启，以便随时发现漏掉的 Codex 设定；而在编辑或校对阶段，可以针对某些条目暂时关闭追踪，避免满屏幕的下划线高亮分散你的注意力。

---

## 2. 自动复数匹配 (Auto-Pluralisation)

对于语言设定为英语变体（English language variant）的小说项目，NovelCrafter 具备自动匹配常见复数形式的功能。

* **规则**：例如，如果你的条目名称是“Goblin”，即使你不单独添加复数作为别名，手稿中出现的“Goblins”也会被系统自动匹配并识别。
* **例外情况**：更复杂的非规则复数形式（例如从“Wolf”变为“Wolves”）则无法被自动匹配。如果遇到这类词汇，你需要手动将它们添加为别名。

---

## 3. 区分大小写匹配 (Case-Sensitive Matching)

默认情况下，NovelCrafter 对条目名称和别名的匹配是**不区分大小写**的。这意味着如果一个条目的名称是“Charm”（护身符），手稿中出现的普通词汇“charm”（魅力）也会被系统匹配并高亮。

如果你需要将特定设定条目与日常普通词汇区分开来，可以启用**区分大小写匹配**：

1. 打开对应的 Codex 条目。
2. 切换到 **Tracking** 选项卡。
3. 启用 **Case-sensitive matching** 选项。

开启后，只有与名称（以及每一个别名）的大小写拼写完全一致的词汇，才会被高亮和计数。

> 💡 **进阶小贴士 (Extra Tip)**
> 
> 此功能对于名称本身就是常见普通名词的条目非常实用。例如：一个名字叫作“Red”的角色，或者一个名叫作“Storm”的地点。

---

## 4. 排除列表 (Exclusion List)

排除列表允许你指定某些特定的词语或短语。即使这些词语与条目名称或别名完全吻合，系统也**不会**将其计为该条目的“提及（Mention）”。

* **应用场景**：如果你的角色名叫作“Will”，但你不希望文本中每一次出现助动词“will”时都被算作该角色的提及，你可以选择开启大小写敏感匹配，或者直接将常见的误报短语（false-positive phrases）添加到排除列表中。

### 如何添加排除词
1. 打开对应的 Codex 条目。
2. 切换到 **Tracking** 选项卡。
3. 在 **Exclusions** 区域下方，输入你想要排除的单词或短语，并用英文逗号分隔。

* **匹配规则**：排除词的匹配规则与条目的名称一致。默认不区分大小写，如果你开启了“区分大小写匹配”，排除词匹配也会随之严格区分大小写。

---

## 5. AI 上下文配置 (AI Context)

**AI Context** 设置可以让你精准控制：该条目的信息是否、以及何时会被作为提示词上下文发送给 AI。

| 选项 (Option) | 行为表现 (Behaviour) |
| :--- | :--- |
| **Always include**<br>（总是包含） | 无论当前选中的文本中是否检测到该条目，它的信息都会被加入 AI 上下文。该选项在此前版本中被称为“全局条目（Global Entry）”。 |
| **Include when detected**<br>（检测到时包含 - 默认） | 当条目的名称或别名在选中的文本、场景节拍（Scene Beats）或 Chat 聊天消息中被检测到时，该条目的内容才会被包含进 AI 上下文。 |
| **Don't include when detected**<br>（检测到时不包含） | 即使文本中检测到了该条目，它也会被排除在 AI 上下文之外。不过，在手动添加场景上下文或通过条目关联（Relations）被间接拉入时，它依然可以被正常调用。 |
| **Never include**<br>（从不包含） | 此条目永远不会被发送给 AI。非常适合用于存放私人笔记、防剧透大纲内容或仅供作者个人参考的背景资料。 |

> ⚠️ **重要信息 (Information)**
> 
> 需要特别注意的是，如果你关闭了上文提到的“追踪（Tracking）”功能，系统也将无法检测并使用“检测到时包含（Include when detected）”这一选项。如果你希望该条目的信息随时可供 AI 调用，但又不希望手稿中出现密密麻麻的下划线高亮，建议将此设置调整为 **Always include（总是包含）**，而不是直接禁用追踪。